<h1>Listing user_projects</h1>

<table>
  <tr>
    <th>User</th>
    <th>Project</th>
    <th>Owner</th>
  </tr>

<% @user_projects.each do |user_project| %>
  <tr>
    <td><%=h user_project.user %></td>
    <td><%=h user_project.project %></td>
    <td><%=h user_project.owner %></td>
    <td><%= link_to 'Show', user_project %></td>
    <td><%= link_to 'Edit', edit_user_project_path(user_project) %></td>
    <td><%= link_to 'Destroy', user_project, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user_project', new_user_project_path %>